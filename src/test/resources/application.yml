spring:
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///fakedb
  jpa:
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.test.yaml
